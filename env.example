# GitHub repository URL (required)
GITHUB_URL=https://github.com/username/repository.git

# GitHub personal access token (optional, for private repos)
GITHUB_TOKEN=

# Branch to clone (optional, defaults to repository default branch)
BRANCH=

# Specific commit hash to test (optional, defaults to latest)
COMMIT_HASH=

# Dependency installation command (optional)
# Examples:
# DEPENDENCY_SETUP=npm install
# DEPENDENCY_SETUP=forge install
# DEPENDENCY_SETUP=npm install && forge build
DEPENDENCY_SETUP=

# Echidna entry point (required)
# Format: path/to/contract.sol --contract ContractName [additional flags]
ENTRY_POINT=test/fuzzing/Fuzz.sol --contract Fuzz
